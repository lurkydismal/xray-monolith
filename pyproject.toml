[project]
name = "xray-monolith"
version = "2026.7.6"
description = "X-Ray Engine is the open-source C++ game engine powering STALKER Anomaly, providing rendering, physics, AI, scripting, audio, networking, and world simulation tailored for large, dynamic open-world gameplay."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lxml>=6.1.1",
    "tqdm>=4.68.4",
]

[dependency-groups]
dev = ["breathe>=4.36.0", "furo>=2025.12.19", "sphinx>=9.1.0"]
